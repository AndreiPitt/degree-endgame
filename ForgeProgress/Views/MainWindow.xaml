<mah:MetroWindow x:Class="ForgeProgress.MainWindow"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ForgeProgress"
        mc:Ignorable="d"
        Title="ForgeProgress" Height="400" Width="700">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        

        
        <StackPanel Margin="10" Grid.Column="0">

            <DatePicker Name="myDatePicker" 
                SelectedDate="{Binding CurrentEntry.Date}"
                DisplayDateStart="2026-01-01" 
                DisplayDateEnd="2030-12-31" 
                FirstDayOfWeek="Monday" 
                Margin="0 0 0 10"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Margin="0 10 0 10" Text="Greutate" />
                <TextBox Grid.Column="1" Text="{Binding CurrentEntry.BodyWeight }" Width="80" Height="10"/>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Margin="0 10 0 10" Text="Total_kcal"/>
                <TextBox Grid.Column="1" Text="{Binding CurrentEntry.TotalCalories }" Width="80" Height="10"/>
            </Grid>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Margin="0 10 0 10" Text="Proteina" />
                <TextBox Grid.Column="1" Text="{Binding CurrentEntry.Protein }" Width="80" Height="10"/>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Margin="0 10 0 10" Text="Carbs" />
                <TextBox Grid.Column="1" Text="{Binding CurrentEntry.Carbohydrates }" Width="80" Height="10"/>
            </Grid>

            <Grid Margin="0 0 0 10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Margin="0 10 0 10" Text="Grasimi" />
                <TextBox Grid.Column="1" Text="{Binding CurrentEntry.Fats }" Width="80" Height="10"/>
            </Grid>

            <Button Content="Adauga"
                Command="{Binding AddEntryCommand}"
                Width="120"
                Margin="0 0 20 15"/>


            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Margin="0 10 0 10" Text="Circumferinta Talie(cm)" />
                    <TextBox Grid.Column="1" Margin="-5 0 0 0" Text="{Binding  CurrentMeasurement.WaistSizeCm}" Width="70" Height="10"/>
                </Grid>
            </StackPanel>

            <Button Content="Adauga masur."
                Command="{Binding AddMeasurementCommand}"
                Width="120"
                Margin="0 10 20 0"/>

        </StackPanel>
        
        
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ListView ItemsSource="{Binding Entries}" Grid.Row="0" Margin="10" Background="CornflowerBlue">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Data" 
                            DisplayMemberBinding="{Binding Date, StringFormat='dd.MM.yyyy'}" 
                            Width="70"/>

                        <GridViewColumn Header="Greutate (kg)" 
                            DisplayMemberBinding="{Binding BodyWeight}" 
                            Width="70"/>

                        <GridViewColumn Header="Proteine (g)" 
                            DisplayMemberBinding="{Binding Protein}" 
                            Width="70"/>

                        <GridViewColumn Header="Grăsimi (g)" 
                            DisplayMemberBinding="{Binding Fats}" 
                            Width="70"/>

                        <GridViewColumn Header="Carbo (g)" 
                            DisplayMemberBinding="{Binding Carbohydrates}" 
                            Width="70"/>

                        <GridViewColumn Header="Total Calorii" 
                            DisplayMemberBinding="{Binding TotalCalories}" 
                            Width="80"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <Grid Grid.Row="1" 
                Margin="0 0 0 10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="4*" />
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ListBox ItemsSource="{Binding Measurements}"
                 SelectedItem="{Binding CurrentMeasurement }"
                 DisplayMemberPath="WaistSizeCm"

                 Grid.Row="0" 
                 Grid.Column="0"
                 Grid.ColumnSpan="2" 
                 Margin="10"
                 Background="BlueViolet"/>

                <Button 
                Grid.Row="1"
                Grid.Column="1"
                Margin="0 0 10 0"
                Content="Salveaza"
                Command="{Binding SaveCommand}"/>
            </Grid>
        </Grid>
    </Grid>
</mah:MetroWindow>
